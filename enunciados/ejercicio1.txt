Ejercicio 1
===========

1.- Ahora mismo el proyecto xml2csv tiene una tarea que transforma un XML a CSV (excluyendo los registros que tienen una edad == 30). Agregar un paso adicional que agregue los registros con edad > 29 a una BD que deberá guardar correspondencia con los campos del fichero XML original.
